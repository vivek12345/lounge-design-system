import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Paragraph } from "../Paragraph";
import { Stack } from "../Stack";

<Meta title="MDX/Paragraph" component={Paragraph} />

# Paragraph

Let's define a story for our `Paragraph` component

`import { Paragraph } from "@zalando/lounge-design-system";`

<Preview withToolbar>
  <Story name="Paragraph">
    <Stack>
      <Paragraph>
        Application metadata are custom string keys and values (max 255
        characters each), set on a per application basis.
      </Paragraph>
      <Paragraph>
        Using Password or MFA grant types with public clients is not
        recommended. To use the Client Credentials grant you have to set a Token
        Endpoint Auth Method other than "none". See our documentation for more
        information.
      </Paragraph>
    </Stack>
  </Story>
</Preview>

<br />
<br />
